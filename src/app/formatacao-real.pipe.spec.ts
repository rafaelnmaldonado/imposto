import { FormatacaoRealPipe } from './formatacao-real.pipe';

describe('FormatacaoRealPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatacaoRealPipe();
    expect(pipe).toBeTruthy();
  });
});
